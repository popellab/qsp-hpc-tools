"""C++ backend integration.

Glue between `qsp-hpc-tools` and the standalone QSP simulator binary
(`qsp_sim`) shipped by SPQSP_PDAC. Handles parameter-XML rendering,
subprocess invocation, and raw-binary trajectory parsing.
"""
